# ml/ — Member A: detection models and data science

Not yet started in this repo. See the individual final report for the full
scope, week-by-week plan, and datasets, and `docs/interfaces.md` for the
three artefacts this track owes `simulation/` and `security-control/`:

1. `ml/feature_spec.json` — frozen feature list, ordered, per tier
2. `ml/minimal_tier_thresholds.json` — the minimal tier as plain rules
3. `ml/metrics/tier_costs.csv` — the inference cost table `AppGraph.java`'s
   `IDS_INFERENCE_MIPS_STUB` is currently standing in for

Branch: `feature/ml-model-training`
